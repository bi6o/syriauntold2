{% extends 'base.html.twig' %}
{% block title %} Infographics : New{% endblock %}
{% block body %}
    <h1>Infographic creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_infographic_city' , { 'id' : infographic.cityId }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
